import { ReactNode } from 'react';
import './globals.css';

export const metadata = {
  title: 'WhatsOmni — AI Omnichannel Marketing, CRM & Booking Platform',
  description: 'Unify WhatsApp, Instagram, Messenger, Telegram, SMS and Email into one workspace.',
};

export default function RootLayout({ children }: { children: ReactNode }) {
  return children;
}
